# Registro de cambios

Formato: una entrada por cambio relevante del modelo o de las definiciones. Las entradas del modelo indican qué versión del `.alp` las contiene.

## [Sin publicar]

### Cambiado

- Modelo, **el round trip del portacontenedor termina en la terminal** (ADR-062). El flujo de los circuitos 1 a 3 pasa a ser `... → viajarPuerto → descargarPuerto → liberarCamion`: se elimina el bloque `retornarDeposito`, que agregaba un **cuarto tramo fisico** (terminal → origen → terminal → origen) que no existe en la operacion. El portacontenedor queda disponible en la terminal, que es donde empieza el ciclo siguiente.
- `envio.tiempoRetornoHoras = 0`. El campo se conserva por compatibilidad de datos, pero ya no representa ningun Delay.
- La **fecha de servicio** del envio es `diaListoEnTerminal` —fin de `descargarPuerto` en los circuitos 1 a 3 y fin de `consolidarCarga` en el circuito 4, que es cuando el contenedor existe— y con ella se fijan `envio.diaEntrega`, `asignacion.diaUltimaEntrega`, las toneladas dentro y fuera del cut-off, `pedido.diaEntrega` y `pedido.diasAtraso`. `finalizarEnvio()` sigue siendo el unico punto de cierre.
- Casos de validacion V-RT-S-01 a V-RT-S-07, medidos con corridas pareadas antes/despues: el ciclo pasa de 9 h a 7 h a 100 km y de 54,4 h a 37,3 h a 1 200 km, y en el caso de dos contenedores por pedido las toneladas dentro del cut-off pasan de 0 a 144 de 288.

### Agregado

- `Envio.diaListoEnTerminal`: el instante fisico en que el producto queda listo en terminal, separado de `diaLlegadaTerminal` (gate-in) y del cierre administrativo.
- `Envio.diaCargosCierre`: el dia de campania en que se devengan los cargos de cierre. `costoEsperadoCircuito()` derivaba la tarifa con `floor(envio.diaEntrega)` mientras el devengo usaba `diaCampania()`, asi que al adelantar la fecha de servicio un cruce de medianoche cortaba la corrida en la auditoria por envio. El servicio es fisico; el cargo es contable.

### Agregado

- Modelo, **flota de producto como camiones discretos con viajes multidiarios** (ADR-061). Un viaje que no entra en una jornada ya puede empezar: el camion queda ocupado hasta que vuelve, el producto sale del origen al salir el viaje y entra al inventario del destino al llegar.
- `UnidadFlotaProducto`: el camion como objeto, con `disponibleDesde`, base, ubicacion actual, viaje en curso, camion-dia acumulado y viajes completados. Es la **unica** fuente de disponibilidad de la flota de producto.
- `ViajeProducto`: el viaje fisico, con origen, destino, lote, pedido, toneladas, fechas de programacion, salida, llegada, inicio de retorno y regreso, duraciones, distancia, estrategia, cross dock, y las banderas de retiro, ingreso y flete. `EstadoViajeProducto` va de `PROGRAMADO` a `COMPLETADO`, con `CANCELADO` para lo que nunca salio.
- `ResultadoDisponibilidadFlota`: lo que la agenda puede prometer **sin mutarla**. Reemplaza el si/no de `flotaProductoAlcanza()`: una alternativa que solo puede mover una parte del volumen compite por esa parte.
- Agenda de flota en `Main`: `inicializarFlotaProducto()`, `duracionIdaProductoDias()`, `duracionRetornoProductoDias()`, `horasManipuleoViajeProducto()`, `duracionTotalViajeProductoDias()`, `buscarCamionDisponibleMasTemprano()`, `fechaSalidaMasTempranaProducto()`, `camionesDisponiblesEn()`, `camionesProductoEnRuta()`, `evaluarDisponibilidadFlotaProducto()`, `programarViajeProducto()`, `programarMovimientoProducto()`, `iniciarViajeProducto()`, `recibirViajeProducto()`, `completarViajeProducto()`, `cancelarViajeProducto()` y los tres pasos diarios `completarViajesProductoDelDia()`, `recibirViajesProductoDelDia()` e `iniciarViajesProductoDelDia()`.
- Inventario en transito como estado real: `toneladasProductoEnTransito`, `toneladasEnTransitoHacia()`, `toneladasComprometidasParaViajesDe()` y `espacioDisponibleEfectivo()`, que descuenta del espacio del deposito lo que ya viene en camino.
- Cota por flota en el evaluador: `acotarAlternativaPorFlota()` y los campos `toneladasFactiblesPorFlota`, `viajesFactiblesPorFlota`, `primeraSalidaProducto`, `ultimaSalidaProducto`, `ultimaLlegadaProducto`, `ultimoRegresoProducto`, `esperaFlotaDias`, `flotaCompleta`, `flotaParcial`, `requiereFlotaProducto` y `diagnosticoFlota` en `AlternativaCircuito`. La fecha estimada de entrega incluye la espera de flota.
- Diagnosticos diferenciados de falta de flota: `SIN_CAMIONES_CONFIGURADOS`, `SIN_FLOTA_ANTES_CUTOFF`, `FLOTA_PARCIAL`, `ESPERA_FLOTA`, `VIAJE_FUERA_HORIZONTE`, `STOCK_NO_RESERVABLE`, `RUTA_SIN_DISTANCIA`, `DURACION_RUTA_INVALIDA` y `CRUCE_SIN_LLEGADA_EN_EL_DIA`.
- **C-04**, reconciliacion diaria de flota (`reconciliarFlotaProducto()`): ningun camion con viajes superpuestos, `disponibleDesde` nunca anterior al ultimo regreso, viajes con carga y dentro de la capacidad, fechas coherentes, nada que ingrese sin haber salido ni se complete sin haber ingresado, y los contadores de transito y de reservado iguales a la suma de los viajes.
- Diecisiete KPIs de flota en el CSV del barrido y en el tablero (viajes programados, iniciados, completados y cancelados; camiones promedio y pico en ruta; espera media y maxima; toneladas reservadas, en transito, no programadas y programadas parcialmente; movimientos parciales; pedidos que perdieron el cut-off con descarte de flota; viajes en curso al cierre).
- Dos parametros nuevos de `Escenario`: `habilita_flota_producto_multidiaria` (default `true`) y `dias_max_programacion_flota` (default `2`), con columnas nuevas en la hoja `Escenario` del libro de ejemplo.
- Casos de validacion V-FLOTA-MD-01 a V-FLOTA-MD-12, con la forma de medicion declarada caso por caso.

### Cambiado

- La transferencia planta→deposito deja de ser instantanea: `transferirToneladasLote()` programa viajes fisicos y el ingreso al deposito ocurre el dia de la llegada, con `dia_ingreso` de la capa igual a esa fecha. El cross dock exige que el viaje **llegue dentro de la jornada**; si no llega, no cruza.
- El flete del tramo se devenga **al salir** el viaje, una sola vez por viaje fisico, y el IN del deposito **al llegar**. Un viaje cancelado antes de salir no paga.
- El evaluador ya no usa `flotaProductoAlcanza()` cuando la agenda esta activa: acota por disponibilidad real y descarta con motivo de flota.
- El pedido cancelado por perder el cut-off (`politica_reprogramacion_buque = CANCELAR`) tambien cancela sus viajes no salidos, que liberan stock reservado y camion.
- `Inventario.validar()` acepta una reserva de viaje sin pedido: una transferencia a deposito mueve inventario propio y no tiene comprador todavia. Toda reserva sigue necesitando dueno.
- El panel *Transporte y flota* del tablero muestra camiones en ruta, pico, viajes iniciados sobre programados, espera media, toneladas en transito y toneladas sin flota cuando la agenda esta activa; con la agenda apagada sigue mostrando el camion-dia de ADR-044.

### Corregido

- `Distancia` declara **un solo sentido por tramo**, asi que el retorno del camion no puede pedir la fila inversa: la lectura pasa a ser simetrica (`distanciaKmSimetrica()`) y un tramo faltante da `RUTA_SIN_DISTANCIA` en vez de abortar la corrida con `Falta la distancia ZARATE -> PLANTA`.

### Agregado

- Modelo, **capacidad finita y menor costo factible** (MOD, ADR-060). La capacidad de posiciones deja de ser diagnóstica y pasa a ser una restricción del plan, resuelta en el orden `capacidad → factibilidad → costo → reserva → asignación`: una alternativa sin posiciones en la ventana se descarta **antes** de compararse por costo, con motivo escrito.
- `ReservaCapacidad`: la posición comprometida como objeto, con `(recurso, sitio, día)`, día original, límite de ventana, reprogramaciones, contenedor asociado, estado y motivo de baja. Los recursos son dos y no comparten cupo: `CONSOLIDACION` y `CROSS_DOCK`.
- Agenda de capacidad en `Main`: `capacidadNominalDia()`, `capacidadReservadaDia()`, `capacidadDisponibleDia()`, `capacidadDisponibleEnVentana()`, `diasDisponiblesEnVentana()`, `reservarCapacidad()`, `consumirReservaCapacidad()`, `liberarCapacidadPorAsignacion()`, `reprogramarReservasCapacidad()` (paso diario, antes del despacho) y `reconciliarCapacidad()` (C-03, invariante diario).
- Campos de capacidad en `AlternativaCircuito` —`capacidadReservable`, `contenedoresConCapacidad`, `toneladasCapacidadDisponible`, `diasCapacidadDisponibles`, `toneladasSinRestriccionCapacidad`, `costoUnitarioSinRestriccion`— y en `ContenedorExportacion` la reserva que va a consumir (`claveReservaCapacidad`, `diaPlanificadoOperacion`, `idUbicacionOperacion`, `tipoRecursoOperacion`).
- Dos parámetros nuevos de `Escenario`: `permite_fallback_politica_fija` (default `false`) y `exportar_diagnostico_capacidad` (default `false`).
- Tres CSV de diagnóstico en `resultados/`: `capacidad_por_dia.csv` (nominal, reservada, consumida, liberada, ocupada, libre y cola por recurso, sitio y día), `asignaciones_capacidad.csv` (una fila por posición, con su historia) y `asignaciones_capacidad_decisiones.csv` (por qué se eligió o se descartó cada alternativa, con el sobrecosto de saturación).
- KPI `sobrecosto_saturacion_usd`: lo que cuesta de más no poder usar la alternativa más barata por falta de posiciones. Es la métrica que hace visible el precio de la restricción en vez de esconderlo como backlog.
- Panel **Capacidad finita** en el tablero de campaña y casos de validación CAP-01 a CAP-16, con la forma de medición declarada caso por caso.

### Cambiado

- `contenedores_por_dia` queda documentado como **capacidad diaria total procesable** del sitio, no como posiciones simultáneas.
- Una posición se ocupa **una sola vez**: reservar ocupa, consumir convierte reserva→consumo sin volver a ocupar y liberar devuelve el cupo. Sin esa regla, reserva y conteo al ejecutar le sacaban la mitad de la capacidad a cada sitio.
- La reserva se toma al crear la **asignación** (`ceil(tn / capacidad)`), no al crear cada contenedor, y el último contenedor parcial consume una posición completa, igual que la paga (ADR-053).
- Las políticas `FIJA_*` respetan la capacidad: el circuito fijo se usa mientras sea factible y, con `permite_fallback_politica_fija`, el saldo que no entra pasa al evaluador en vez de perderse.
- `datos/entrada_ejemplo.xlsx`: la hoja `Escenario` suma las dos columnas nuevas.

### Corregido

- Un pedido cancelado por perder el cut-off (`politica_reprogramacion_buque = CANCELAR`) **seguía bloqueando las posiciones** que tenía tomadas: nadie llamaba a `liberarCapacidadPorAsignacion()`. Ahora la cancelación libera las posiciones con motivo. La ruta no se activó en los libros CAP y así queda anotado en el plan de validación.

### Agregado

- Modelo, **ventana marítima y cut-off** (MOD, ADR-059), versión `fase-26`. Un pedido deja de tener una sola fecha límite y pasa a tener cuatro: `dia_conocimiento` (desde cuándo existe, reserva, asigna, transfiere y planifica), `dia_apertura_retiro_vacio` (primer día en que el vacío puede salir de la terminal), `dia_cutoff_fisico` (último ingreso del cargado a terminal) y `dia_etd` (zarpada). El contrato exige `conocimiento <= apertura <= cut-off <= etd` y el importador lo valida fila por fila.
- Metadatos marítimos del pedido en el contrato: `naviera`, `incoterm`, `buque` y `viaje_buque`. `buque` es lo que agrupa el cluster real de cut-off, así que el servicio se puede leer por buque y no sólo por pedido.
- Siete parámetros nuevos de `Escenario`: `dias_anticipacion_planificacion_default` (14), `dias_anticipacion_retiro_default` (7), `dias_entre_cutoff_y_etd_default` (1), `permite_reserva_antes_retiro`, `permite_transferencia_antes_retiro`, `permite_reserva_capacidad_futura` (los tres en `true`) y `politica_reprogramacion_buque` (`CONTINUAR`).
- `actualizarVentanasRetiroDelDia()` como paso 2b del día, inmediatamente después de registrar los pedidos: es el único punto que abre la ventana y pasa los contenedores de `CREADO` a `ESPERANDO_PROGRAMACION`. Y `registrarPerdidaDeCutoff()` como paso 11b, después de `registrarAtrasos()` y antes de las validaciones. El orden de los pasos existentes no cambia.
- `tiempoLogisticoEstimadoDias()`, `tiempoLogisticoMinimoDias()` y `horasCicloFisico()`: el tiempo del ciclo físico deja de estar implícito en el flujo y se puede preguntar antes de comprometer, que es lo que permite detectar una ventana inviable el día en que abre.
- `planificarPosicionFutura()` y `posicionesPlanificadas`: registro de la posición de consolidación que cada contenedor va a necesitar dentro de su ventana. Es planificación, no consumo: no toca la capacidad operativa del día.
- Nueve KPIs nuevos en `resultados/kpis_por_corrida.csv`: `servicio_toneladas_cutoff`, `toneladas_dentro_cutoff`, `toneladas_fuera_cutoff`, `pedidos_perdieron_cutoff`, `buques_cumplidos`, `buques_perdidos`, `holgura_promedio_dias`, `pedidos_ventana_inviable` y `contenedores_sin_posicion_futura`.
- Panel **Ventana marítima y cut-off** en el tablero de campaña, con conocidos y ventanas abiertas, pérdidas de cut-off, ventanas inviables, buques cumplidos y perdidos, holgura media, toneladas dentro y fuera del cut-off, y contenedores planificados sin ejecutar o sin posición futura.
- Casos de validación VM-01 a VM-11, con la forma de medición declarada caso por caso (corrida directa, barrido o lectura de código).
- `datos/entrada_ejemplo.xlsx` adaptado al contrato nuevo: `PedidoPlan` pasa a las 13 columnas canónicas —`dia_llegada` y `dia_limite` desaparecen del libro— y `Escenario` suma las siete columnas nuevas. Las cuatro fechas van explícitas (`cut-off = dia_limite` anterior, apertura = cut-off − 7, conocimiento = `dia_llegada` anterior, ETD = cut-off + 1) y `buque`/`viaje_buque` se poblan por cluster de cut-off × terminal (1 399 pedidos, 409 buques). La hoja `StockInicial` se conserva.

### Cambiado

- **El servicio se mide contra `dia_cutoff_fisico`, no contra el ETD.** El ETD queda como dato de la salida marítima y no participa del cumplimiento operativo.
- La compatibilidad con libros anteriores es explícita: con `dia_llegada`/`dia_limite` la ventana se deriva (`cut-off = dia_limite`, `apertura = max(dia_llegada, cut-off − dias_anticipacion_retiro_default)`, `conocimiento = dia_llegada`, `etd = cut-off + dias_entre_cutoff_y_etd_default`), la corrida sigue y la consola avisa que la ventana es derivada. La derivación nunca inventa conocimiento más temprano que el que el libro declara.
- El CSV pasa a `fase-26` y los KPIs se mueven en todos los escenarios **por diseño**: antes un pedido con 31 días de plazo se ejecutaba desde el día 1, ahora la ejecución arranca cuando abre el retiro. E-00 baja de 0,975 a 0,696 de servicio con las mismas toneladas exportadas; E-05 (0,988) y E-22 (0,990) casi no se mueven porque ya tenían margen. Con `datos/entrada_ejemplo.xlsx` el servicio queda en 26 %, y correr el mismo libro con la apertura igualada al conocimiento (la regla anterior) da 32 %: la ventana explica unos 6 puntos y el resto es el techo estructural de ADR-044 ya declarado.
- No se agregó ningún estado de contenedor: la planificación anticipada usa el `CREADO` que ya existía.

- Modelo, **stock inicial de campaña** (MOD, ADR-057), versión `fase-25`. La disponibilidad deja de ser sólo la producción simulada y pasa a ser stock inicial más producción. El inventario preexistente entra como **capas reales** de `Inventario`, con lotes `esStockInicial = true` y `codigoLoteExterno`, no como producción ficticia, transferencia ficticia, pedido especial ni saldo paralelo: el FIFO, las reservas parciales, el cross dock, el almacenaje y las conciliaciones lo ven sin ninguna rama especial.
- Hoja `StockInicial` **opcional** en el contrato de datos (`id_escenario`, `id_stock`, `codigo_lote`, `producto`, `id_ubicacion`, `toneladas`, `dia_produccion`, `dia_ingreso`, `cliente`, `calidad`), leída por `ImportadorExcel` y presente en la plantilla que genera `tools/generar_excel_ejemplo.py`. Reemplaza a la tabla `LoteInicial` que el contrato proyectaba. Es opcional para que los libros anteriores —incluido `datos/entrada_ejemplo.xlsx`— sigan corriendo: ausente equivale a inventario inicial cero.
- `Main.cargarStockInicial()` y `Main.validarStockInicial()`. La carga corre en el arranque del agente, después de `cargarDatosEntrada()` y antes del primer `pasoDiario_accion()`, así que el stock está disponible para los pedidos del día 0. La identidad del lote histórico es `codigo_lote + producto + cliente + calidad`, un mismo lote puede estar en varias ubicaciones, y los lotes iniciales quedan cerrados y con `toneladasObjetivo = 0`, así que no reciben producción de campaña.
- Validación con el criterio de ADR-037 (todos los errores juntos, el arranque se detiene): identidad, cantidad, ubicación (`PLANTA` o `DEPOSITO`, habilitada, con capacidad para el producto) y fechas (`dia_produccion <= 0`, `dia_ingreso <= 0`, `dia_ingreso >= dia_produccion`) en `DatosEntrada.validar()`; capacidad efectiva en `Main.validarStockInicial()`, dura en depósito y **advertencia en planta**, porque la capacidad nominal del frío propio es un umbral de lectura y arrancar en sobrecarga es un dato real (ADR-048).
- Siete KPIs nuevos en `resultados/kpis_por_corrida.csv`: `stock_inicial_tn`, `stock_inicial_consumido_tn`, `stock_inicial_remanente_tn`, `produccion_campania_tn`, `disponibilidad_total_tn`, `demanda_planificada_tn` y `deficit_estructural_tn`. El déficit estructural es `max(0, demanda − stock inicial − producción planificada)` por producto: con `datos/entrada_ejemplo.xlsx` da 3 924 tn, así que el nivel de servicio de ese escenario se lee contra ese techo y no contra el 100 %.
- Línea de stock inicial en el tablero de campaña y ADR-057, más los casos de validación SI-01 a SI-12.
- `datos/entrada_ejemplo.xlsx` trae la hoja `StockInicial` en el formato canónico del contrato (§6.4): las diez columnas y una fila por partida, con los datos del relevamiento (12 filas, 5 771 tn). Las fechas van en 0 porque el relevamiento no tiene antigüedad y no se inventa. Ojo: `python3 tools/generar_excel_ejemplo.py` regenera la plantilla desde el escenario sintético y **sobrescribe** esta hoja.
- Lectura tolerante de la hoja de stock inicial (ADR-058), en `ImportadorExcel.leerStockInicial()`. Es la única hoja que escribe una persona y no el volcado del modelo, así que se acepta el nombre de hoja con erratas (`StockcInicial`), encabezados alternativos (`DEPOSITO`, `Inicial`), ubicaciones escritas como en el negocio (`RUTA 9` → `RUTA9`, `DODERO BARRACAS` → `DODERO`) y las columnas de identidad y fechas ausentes, con defaults explícitos. Habilita la forma agregada `producto × depósito` que se usa para relevar existencias: `datos/entrada_ejemplo.xlsx` carga 5 771 tn en 12 lotes iniciales. La tolerancia es de formato: producto, ubicación y toneladas siguen siendo obligatorios y lo que no resuelve aborta el arranque con la fila.

### Corregido

- `toneladaDiaEnStock()` medía la antigüedad de una capa como `time() − diaIngreso`, así que una capa de stock inicial con `dia_ingreso = -60` arrancaba el día 0 con 60 tn-día de almacenaje ya imputados. Es almacenaje anterior al horizonte —costo hundido que nadie va a pagar— y hacía que la política end-to-end evitara el stock inicial. La antigüedad se acota al horizonte: `time() − max(0, diaIngreso)`. Con `dia_ingreso >= 0` es la identidad, y por eso el barrido sin stock inicial no se mueve un centavo.
- `panelProduccionLinea5`, la línea nueva del tablero, quedó anidada dentro de la línea 4 en la primera edición del `.alp`: el XML seguía siendo válido pero AnyLogic no la dibujaba. El utilitario de edición ahora inserta después del cierre real del elemento.

### Cambiado

- **C-02 pasa a ser la identidad de disponibilidad**: `stock inicial + producción = stock en planta + stock en depósitos + en proceso + entregado`, tolerancia 0,001 tn. Con la identidad anterior —sólo producción de planta— la corrida abortaba el día 0 en cuanto había stock inicial, porque el inventario era mayor que lo producido.
- `LoteProducto` agrega `codigoLoteExterno` y `esStockInicial`. En un lote inicial `toneladasIniciales` es el total histórico cargado y no producción de campaña; sigue siendo el acumulador monótono de ADR-047 y el origen se distingue con `esStockInicial`.
- El CSV pasa a `fase-25`. Sin stock inicial el barrido es **idéntico** a `fase-24`: las 1 080 corridas coinciden en las 55 columnas comunes, sin una sola diferencia.

### Agregado

- Modelo, **pedidos parciales con múltiples orígenes** (MOD, ADR-055), versión `fase-24`. La unidad de compromiso deja de ser el pedido y pasa a ser la asignación: `AsignacionPedido` (clase Java plana, el modelo sigue en 10 de 10 tipos de agente de PLE) guarda origen, circuito, condición de cross dock y el ciclo completo en toneladas (asignadas, reserva activa, contenerizadas, despachadas, entregadas), con clave de reserva propia `codigoPedido + "|" + idAsignacion`.
- `Main.reservarParcialPedido()` y `asignarParcialPedido()`: se reserva lo que cada origen pueda dar y **se conserva**, en lugar de revertir todo porque el pedido no queda cubierto. El saldo vuelve a competir al día siguiente. Las políticas `FIJA_*` conservan su orden de candidatos; las económicas ordenan con el evaluador (ADR-054).
- `Main.crearContenedoresParaAsignacion()`: contenedorización progresiva por asignación. Se arma un contenedor cada vez que la reserva viva alcanza para uno completo, y el último parcial se difiere hasta que el pedido esté completamente asignado, venza o termine la campaña, porque en el contrato de costos un parcial paga como uno lleno (ADR-053).
- Despacho y entrega por clave de reserva, con entrega parcial real: `ENTREGADO` sólo cuando se cubre el total, y `ATRASADO` conservando lo entregado, las asignaciones vivas y el origen de cada fracción.
- Modelo, **componente preventivo en la política FLEXIBLE** (MOD, ADR-056). `toneladasASacarPreventivamente()` compara el stock proyectado —físico más forecast de producción— contra el umbral de alerta y, si lo toca, baja hasta el umbral objetivo. Se combina con los componentes de desborde y de servicio mediante `max`, nunca por suma, y queda limitado por el stock libre.
- `Main.transferirProductoADepositos()`: el objetivo de transferencia se **reparte** entre todos los depósitos factibles. Una transferencia de 300 tn ya no se detiene porque en el primer depósito entren 100: el destino se marca agotado por hoy y el bucle sigue con el siguiente hasta cubrir el objetivo, agotar el stock libre o agotar los candidatos.
- `Main.diagnosticoDepositos()`, `diagnosticoPedido()` y el parámetro `debugPlanificacion`: el diagnóstico usa la **misma** función de selección que la decisión, así que el motivo informado (`NO_HABILITADO`, `SIN_CAPACIDAD_PRODUCTO`, `SIN_ESPACIO`, `TARIFA_INEXISTENTE`, `TARIFA_SITIO_INEXISTENTE`, `SIN_FLOTA`) es el motivo real. `DatosEntrada.hayTarifaFlete()` y `hayTarifaSitio()` responden si existe una tarifa vigente sin abortar la corrida.
- `Main.validarBalancePedidos()` (C-01) y `validarBalanceProducido()` (C-02), ejecutados todos los días con tolerancia 0,0001 tn: el pedido tiene que cerrar como pendiente de asignar + reserva activa + despachado no entregado + entregado, y lo producido tiene que estar en planta, en depósitos, en proceso o entregado.
- Trece KPIs nuevos en `resultados/kpis_por_corrida.csv`: `pedidos_parcialmente_reservados`, `pedidos_multi_origen`, `pedidos_parcialmente_entregados`, `pedidos_atrasados_entrega_parcial`, `asignaciones_creadas`, `asignaciones_parciales`, `toneladas_pendientes_asignar`, `toneladas_pendientes_entregar`, `toneladas_transferidas_preventivas`, `toneladas_transferidas_desborde`, `toneladas_transferidas_servicio`, `toneladas_transferidas_criticas` y `transferencias_incompletas`.
- ADR-055, ADR-056 y los casos de validación P-01 a P-04, T-01 a T-07, E-01, E-02, C-01 y C-02.

### Corregido

- El almacenaje decidía qué no pagar comparando la reserva contra el código de pedido. Con la clave por asignación eso habría hecho que el cross dock empezara a pagar almacenaje en silencio, así que la reserva de la capa guarda **dos** identificadores: la clave `pedido|asignación`, que define qué consume un despacho, y el `codigoPedido`, que sigue gobernando las reglas comerciales.
- `pedidos_parcialmente_reservados` y `pedidos_parcialmente_entregados` se medían sobre el estado final, donde todos los pedidos ya cerraron, así que daban 0 en las 1 080 corridas. Pasan a ser marcas históricas del pedido. La de entrega parcial se evalúa al cierre del día y no al llegar cada envío: un pedido se despacha en varios contenedores, y contar cada uno hacía que el KPI diera 40 de 40 pedidos, midiendo la contenedorización en lugar de la entrega parcial.
- `colaCamiones` (la cola de envíos que esperan portacontenedor) usaba la capacidad por default de la librería (100 agentes) y con la demanda del Excel actualizado la corrida abortaba en el día 162 con "An agent was not able to leave the port root.entradaEnvios.out". Esa cola es una lista de espera, no una restricción física —la restricción es la flota—, así que pasa a capacidad ilimitada. No cambia ningún número del barrido sintético: las 1 080 corridas dan idénticas en las 55 columnas.
- `tools/alp.py` (utilitario de edición) cambiaba `<ReturnType>` sin cambiar `<ReturnModificator>`, y AnyLogic toma el modificador como determinante: una función con retorno quedaba declarada `VOID` y el modelo no compilaba ("Void methods cannot return a value"). Ahora escribe los dos.

### Cambiado

- `esCrossDock` deja de ser un atributo del pedido y pasa a la asignación y al envío: una fracción puede cruzarse y otra almacenarse, lo que destraba el cross dock parcial.
- `Pedido.toneladasReservadas`, `idSitioOrigen` y `esCrossDock` quedan como campos **legacy** de presentación y compatibilidad. Los saldos se derivan de las asignaciones (`toneladasAsignadasAcumuladas()`, `toneladasReservadasActivas()`, `toneladasEnProceso()`, `toneladasPendientesAsignar()`, `toneladasPendientesEntregar()`, `estaCompleto()`).
- `pasoDiario_accion()` conserva su orden; sólo se agregan las dos validaciones de balance después de `validarInventario()`.
- `toneladasASacarReactiva()` **no** se modificó: es idéntica carácter por carácter a la de `fase-23`, y E-12 no transfiere una sola tonelada por el componente preventivo. Su costo por tonelada sí se mueve +0,1 % por el otro cambio del mismo MOD, los pedidos parciales, que afectan a todos los escenarios: la igualdad bit a bit que pedía el criterio T-06 original no es alcanzable con los dos cambios en la misma versión.
- Las series de costo de `fase-23` no son comparables con `fase-24`: el componente preventivo mueve producto a depósito antes de necesitarlo y el costo por tonelada sube entre 6,8 % y 16,6 % según escenario. Lo físico sí se sostiene: toneladas exportadas y stock final coinciden.

### Agregado

- Modelo, evaluador de circuitos por pedido (C5+C6, ADR-054), versión `fase-23`. `PlanLogistico` deja de estar muerto: con una política económica, cada pedido genera alternativas a partir del stock real —consolidación en el sitio donde está el producto, cross dock por depósito habilitado y circuito de terminal—, se descartan las no factibles con motivo escrito, se costean por componente y se elige la mejor por servicio y después por costo. La elegida se ejecuta con el flujo físico que ya existía, no con un camino paralelo.
- `AlternativaCircuito`: clase Java plana (el modelo sigue en 10 de 10 tipos de agente de PLE) con origen, sitio de estiba, circuito, factibilidad, motivo, fecha estimada, servicio proyectado y los componentes de costo, más las vistas `costoIncremental`, `costoHistorico` y `costoEndToEnd`.
- `Main.asignarConEvaluador`, `evaluarAlternativa`, `costearAlternativa`, `costearHundidoAlternativa`, `horasCicloAlternativa`, `ordenarAlternativas`, `ejecutarAlternativa` y `registrarPlan`. La factibilidad **no muta inventario**: evaluar no reserva.
- `politica_seleccion` y `servicio_minimo_proyectado` en el contrato de datos. Las políticas `FIJA_*` y `MANUAL` reproducen la conducta previa al evaluador y son la regresión; `PRIORIDAD_FRIO_PROPIO`, `MENOR_COSTO_INCREMENTAL_FACTIBLE` y `MENOR_COSTO_END_TO_END_FACTIBLE` lo activan.
- Siete factores de escenario nuevos: `factor_tarifa_flete`, `factor_tarifa_round_trip`, `factor_tarifa_cross_dock`, `factor_tarifa_terminal`, `factor_consolidacion_planta`, `factor_cupo_cross_dock` y `factor_capacidad_terminal`, todos con default 1.
- 22 escenarios (E-14 a E-35): estrategia de decisión, sensibilidad tarifaria ±20 %, permanencia de 7/15/30/45/60 días y capacidad reducida de frío propio, consolidación en planta, cross dock y terminal. El barrido pasa de 420 a 1 080 corridas.
- Cinco KPIs de decisión en `resultados/kpis_por_corrida.csv`: `planes_emitidos`, `planes_tardios`, `alternativas_evaluadas`, `alternativas_descartadas` y `pedidos_sin_alternativa_factible`, más la política de selección en la configuración que el barrido imprime por escenario.
- ADR-054 y los diez casos de validación V-DEC-01 a V-DEC-10.

### Corregido

- Los factores de sensibilidad tarifaria se aplicaban en la cotización pero no en el devengo: el cobro leía el campo crudo de la fila de tarifa mientras la auditoría por envío leía el accesor con el factor, así que con cualquier factor distinto de 1 la corrida abortaba (E-20: round trip devengado 390,0 contra 312,0 cotizado). El devengo pasa a usar los mismos accesores que la auditoría, de modo que los ocho escenarios de sensibilidad miden lo que dicen medir.

### Cambiado

- Con el evaluador activo los pedidos pendientes se ordenan por fecha límite y código antes de asignarse, para que la decisión no dependa del orden de población de la lista. Con política fija el orden anterior se conserva intacto.

- Modelo, contrato de costos con unidad, proveedor y vigencia mensual (C1, ADR-051). Cuatro tablas —`TarifaFleteProducto`, `TarifaRoundTrip`, `TarifaSitio` y `TarifaEspera`— reemplazan a `TarifaAlmacenamiento`, `TarifaServicioCarga`, `TarifaCicloContenedor`, `TarifaTerminal`, `TarifaTHC` y `TarifaDespachante`. Toda tarifa lleva `proveedor`, `vigencia_desde`, `vigencia_hasta` y `habilitada`, y las consultas `DatosEntrada.tarifaFlete/tarifaRoundTrip/tarifaSitio/tarifaEspera` resuelven por **día de campaña**: si no hay fila vigente, o hay dos para la misma clave, la corrida aborta con la clave y el día en el mensaje, en lugar de devolver un cero silencioso.
- `DatosEntrada.importe(unidad, tarifa, toneladas, contenedores, motivo)` y los envoltorios `importeFlete`, `importeConsolidacion`, `importeCrossDock` y `roundTripUsdContenedor`: la unidad de la fila (`USD_VIAJE`, `USD_TN`, `USD_CONTENEDOR`, `USD_TN_DIA`, `USD_HORA`, `USD_OPERACION`, `USD_PEDIDO`) decide la base de cálculo. El flete de producto acepta las dos unidades en la misma tabla, con `variable_usd_tn` opcional junto a la tarifa por viaje.
- Modelo, registro auditable de cargos (C2, ADR-052). `RegistroCostos` (clase Java, no un tipo de agente: el modelo sigue en 10 de 10 de PLE) guarda un `Cargo` inmutable por evento económico, con identidad, día, categoría, tipo (`CAJA` o `ECONOMICO`), pedido, contenedor, lote, producto, origen, destino, sitio, estrategia, proveedor, unidad, cantidad, tarifa e importe **calculado** como `cantidad × tarifa`. `registrar()` es idempotente por operación, categoría, unidad y motivo, y devuelve lo que efectivamente entró al registro.
- `Main.reconciliarCostos()`: compara cada acumulador de agente contra el total de su categoría en el registro, todos los días y al cierre, y aborta la corrida si difieren en más de 0,01 USD. El barrido lo ejecuta antes de escribir los KPIs de cada corrida.
- `RegistroCostos.exportarCsv(ruta)`: volcado opcional del detalle para auditar un número. No se llama en el barrido, porque una campaña son cientos de miles de cargos.
- `Capa.idCapa`, asignada por `Inventario`: la capa necesita identidad propia porque un lote puede tener varias capas en el mismo depósito y el cargo de almacenaje tiene que poder apuntar a una y sólo una.
- ADR-051, ADR-052 y casos de validación V-029 y V-030.
- Modelo, costeo por circuito (C3, ADR-053), versión `fase-22`. Se devengan los seis conceptos que estaban en 0: IN cuando la capa entra al almacenamiento, OUT cuando el envío se despacha, THC y costo de terminal cuando el contenedor cargado entra a la terminal —o cuando se arma ahí, en el circuito de terminal—, despachante por contenedor o por pedido según la unidad, y espera de camión de producto y de portacontenedor por las horas que superan la franquicia. Consolidación y cross dock pasan a cobrarse por contenedor completo, así que el último contenedor parcial paga como uno lleno.
- `Main.costoEsperadoCircuito(envio)` y `exigirIgual(...)`: la auditoría de costeo por circuito reconstruye el importe desde las tarifas, sin mirar el registro, y `finalizarEnvio()` aborta la corrida si no coincide con lo devengado. Ejecuta V-COST-01 a V-COST-05 y V-COST-07 en cada envío de cada corrida (≈ 222 000 auditorías en el barrido).
- `Main.costoEndToEndPedido`, `costoIncrementalPedido` y `costoHistoricoPedido`: las tres vistas de costo del pedido, sobre el registro. La comparación entre alternativas usa la incremental, porque el almacenaje y el flete ya incurridos son costo hundido.
- Once columnas de descomposición por categoría en `resultados/kpis_por_corrida.csv` (`costo_flete_producto_usd`, `costo_round_trip_usd`, `costo_consolidacion_usd`, `costo_cross_dock_usd`, `costo_almacenamiento_usd`, `costo_in_usd`, `costo_out_usd`, `costo_thc_usd`, `costo_terminal_usd`, `costo_despachante_usd`, `costo_espera_usd`) y las mismas líneas en el panel de costos de `Main`.
- `DatosEntrada.tarifaEspera` y `horasEsperaFacturables` como consultas separadas del importe, y una validación nueva: la franquicia y la tarifa de espera de `TarifaRoundTrip` tienen que coincidir con la fila de `TarifaEspera` del mismo sitio, o la corrida aborta.
- Tarifas de referencia marcadas como supuesto con proveedor `SUPUESTO_C3`: IN y OUT (2,0–3,0 USD/tn), THC (150–220 USD/contenedor), costo de terminal (70–90), despachante (120) y espera (3 h, 25 USD/h). Se reemplazan en el Excel sin tocar código.
- ADR-053 y los diez casos de validación V-COST-01 a V-COST-10.

### Corregido

- El flete de producto y el round trip dejaban de cobrarse con las tarifas del contrato: lo que se devengaba eran tres parámetros cableados en `Main` (`costoFijoViajePD + distancia * 2 * costoKmPD`), y la tabla se leía sólo para *estimar* y elegir depósito. Cambiar una tarifa en el Excel cambiaba qué depósito se elegía pero **no** el costo de campaña. Desde C1 el cobro sale de la tabla, así que los costos del barrido responden a los datos; que en `fase-21` el circuito de depósito no se moviera un centavo no era estabilidad, era insensibilidad.

### Cambiado

- Los totales dejan de ser una suma de acumuladores: `costoTotalCampania()` devuelve `registro.total(CAJA)` y el económico `registro.total()`; los acumuladores de `Main`, `Deposito` y `Terminal` quedan como vistas derivadas y el tablero muestra vistas del mismo registro (ADR-052).
- **Los números no se mueven, y eso es el criterio de aceptación de C1+C2:** barrido completo de 420 corridas comparado fila por fila contra `fase-21`, con las 26 columnas de KPI idénticas en las 420 filas salvo una diferencia de 1 × 10⁻⁴ USD en `costo_total_economico_usd` de E-00 réplica 29, que es orden de suma de punto flotante. La corrida desde `datos/entrada_ejemplo.xlsx` da el mismo total que la sintética (USD 1.694.024 y 142,1 USD/tn en E-00 réplica 0).
- **Un libro de Excel anterior a C1 no importa:** `TarifaAlmacenamiento` y `TarifaServicioCarga` se reemplazan por `TarifaSitio`, se agregan `TarifaRoundTrip` y `TarifaEspera`, y toda tarifa pide `proveedor`, `vigencia_desde`, `vigencia_hasta` y `habilitada`. Regenerar con `python3 tools/generar_excel_ejemplo.py`.
- Quedan con estructura, tarifa y consulta pero **sin devengo** hasta C3: IN, OUT, THC, costo terminal, despachante y espera, todos en 0. Hasta entonces el costo por tonelada no es comparable contra una cotización de un tercero.

### Agregado (fase 21 y anteriores sin publicar)

- Modelo, flujo físico con los cuatro circuitos (fase 21, ADR-050). El flowchart de `Main` deja de ser lineal: `seleccionarCircuito` (`SelectOutput`, condición `usaPortacontenedor(envio)`) separa los circuitos que usan portacontenedor de los que no. Los circuitos 1 a 3 (consolidación en planta, en depósito y cross docking en depósito) agregan `viajarVacioAlOrigen` y hacen el round trip terminal → origen → terminal → origen, con la estiba en `cargarCamion`, es decir en el sitio real; el circuito 4 (consolidación / cross docking en terminal) usa `cargarGranel → viajarTerminalGranel → descargarTerminal → consolidarCarga`, viaja a granel consumiendo flota de producto y **no** toca el pool de portacontenedores. Verificado en PLE: build limpio, campaña de 183 días sin excepciones y 420 corridas `Finished`.
- `Main.circuitoDe(idSitioOrigen, esCrossDock)`, `Main.usaPortacontenedor(envio)`, `Main.registrarEstibaEnOrigen(envio)`, `Main.registrarIngresoTerminal(envio)`, `Main.registrarConsolidacionEnTerminal(envio)` y `Main.finalizarEnvio(envio)`: el circuito se decide por pedido y se guarda en `Pedido.estrategiaSeleccionada` y `Envio.circuito`; `estrategiaConsolidacion` queda como política por defecto de la corrida. Es lo que permite enchufar después el evaluador `PlanLogistico` sin volver a tocar el flujo.
- `Pedido.idSitioOrigen` y `Envio.idSitioOrigen`: el origen del despacho es un `id_ubicacion` (`"PLANTA"` o el id del depósito) y no un `Deposito`. `reservarLotesParaPedido(pedido, idSitio)`, el despacho, las distancias, los fletes y la tarifa de estiba se resuelven contra ese id, así que un pedido puede salir del frío propio sin pasar por un tercero. `depositoAsignado` y `depositoOrigen` se conservan como referencia derivada (`null` si el origen es la planta).
- Datos operativos de la planta en el generador sintético y en la plantilla de Excel: `contenedores_por_dia`, velocidades de carga y estiba, tarifa de estiba y filas de `Distancia` y `TarifaFlete` planta → terminal. Hasta `fase-19` la fila `PLANTA` venía en cero, así que el circuito de planta habría salido gratis e instantáneo. Son valores equivalentes al depósito de referencia y están marcados como supuesto en el contrato de datos.
- Escenario E-13 (`CONSOLIDACION_PLANTA`) y cinco KPIs por circuito en el CSV y en el tablero de `Main`: `contenedores_circuito_planta`, `contenedores_circuito_deposito`, `contenedores_circuito_cross_dock`, `contenedores_circuito_terminal` y `viajes_granel_terminal`. El barrido pasa a 14 escenarios × 30 réplicas = 420 corridas y el CSV se etiqueta `fase-21`.
- ADR-050 y casos de validación V-027 y V-028.

### Cambiado

- Las series del barrido anteriores a `fase-21` no son comparables en los escenarios donde el pool de portacontenedores es escaso ni en el circuito de terminal. Medias de 30 réplicas: el circuito de depósito no cambia de costo (E-00 sigue en USD 1.573.338 y 124,37 USD/tn) pero sube la ocupación del pool de 11 % a 13 % porque el tramo vacío es real; E-01 (1 portacontenedor) cae de 0,98 a 0,77 de nivel de servicio y sube de 0,49 a 2,06 días de atraso, que es el round trip pagándose donde hay escasez; E-11 (terminal) pasa de 11 % a 0 % de utilización de portacontenedor y sube 1,1 % de costo porque esas toneladas ahora pagan flete de producto hasta la terminal. E-13 entrega 121,29 USD/tn contra 124,37 de E-00, pero con 0,90 de nivel de servicio: el cuello es `contenedores_por_dia` de la planta.

- Modelo `RedLogistica_Exportacion.alp` versionado en el repositorio.
- `tools/exportar_modelo.py` y `model_src/`: espejo legible del modelo para poder revisar cambios de lógica en un pull request (ADR-035).
- [Inventario del modelo real](docs/03_Logica/Inventario_del_Modelo.md) con los hallazgos H-01 a H-14 y los límites de PLE verificados contra el modelo.
- ADR-030 a ADR-035: estructuras de datos como clases Java, presupuesto de tipos de agente, barrido con Parameter Variation, separación entre parámetros y estado, secuencia diaria explícita y versionado del espejo del modelo.
- Casos de validación V-019 a V-022.
- Clases Java `DatosEntrada` y `GeneradorSintetico` en el modelo (ADR-036): las tablas de entrada del contrato de datos y el generador que las puebla con semilla reproducible.
- `Main.cargarDatosEntrada()`, `Main.aplicarDatosAAgentes()` y `DatosEntrada.validar()`: el modelo genera, valida y distribuye las tablas antes del día 1 (ADR-037).
- Parámetros de escenario en `Main`: `idEscenario`, `duracionCampaniaDias`, `semillaBase`, `variabilidadProduccion`, `variabilidadDemanda`, `pedidosPorCampania`, `toneladasMediasPedido` y `plazoPedidoDias`.
- ADR-036 y ADR-037.
- `tools/exportar_modelo.py` exporta también las clases Java del modelo a `model_src/`.
- Clase Java `ImportadorExcel` (ADR-038): carga las tablas de `DatosEntrada` desde un libro `.xlsx` con una hoja por tabla, buscando las columnas por nombre. Informa juntas las hojas y columnas faltantes, saltea filas vacías y filtra por `id_escenario` las hojas que lo tienen, de modo que un libro puede contener varios escenarios.
- Parámetros `origenDatos` (`SINTETICO` o `EXCEL`) y `rutaExcel` en `Main`, y lista de opciones `OrigenDatos`: el origen de los datos se cambia sin tocar código.
- `datos/entrada_ejemplo.xlsx` y `tools/generar_excel_ejemplo.py`: plantilla del libro de entrada, generada corriendo el propio `GeneradorSintetico` del modelo para que la plantilla y la fase sintética no puedan divergir. Verificado en PLE: el mismo escenario cargado desde Excel y generado sintéticamente produce los mismos 494 envíos, 40 pedidos entregados y los mismos costos.
- Clases Java `Capa` e `Inventario` (ADR-021, ADR-030) y la variable `Main.inventario`: la unidad física del inventario es la capa `(lote, producto, ubicación, día de ingreso, toneladas, reservas)`. `Inventario` resuelve stock, reservado y libre por ubicación y por lote, consume en FIFO por día de ingreso (ADR-022), mueve capas entre ubicaciones con retiro parcial y anota las reservas por pedido.
- `Main.transferirToneladasLote(lote, destino, toneladas)` y `Main.actualizarUbicacionLote(lote)` (fase 4): la transferencia planta–depósito pasa a ser por toneladas y devuelve lo efectivamente movido. `Inventario` suma `stockLoteEn`, `libreDeLoteEn`, `ubicacionesDeLote` y `ubicacionPrincipalDeLote` para poder responder dónde está un lote que está en varios lugares.
- `Main.crearContenedoresParaPedido(pedido)` y `Main.contarContenedores(estado)` (fase 6): la carga de un pedido se divide en contenedores reales de la capacidad del tipo que corresponde al producto, con el último parcial, y cada contenedor guarda el lote que más aporta a su carga. `Inventario.capasReservadasDe(...)` devuelve las capas reservadas del pedido en orden FIFO, que es lo que permite esa trazabilidad. En pantalla aparece la cantidad de contenedores y cuántos se exportaron.
- `Main.consolidaEnDeposito()`, `Main.sitioConsolidacion(contenedor)`, `Main.abrirPosicionesConsolidacionDelDia()`, `Main.tomarPosicionConsolidacion(idUbicacion)` y `Main.despacharContenedoresPendientes()` (fase 7, ADR-039, ADR-040): consolidar pasa a consumir un recurso finito. Cada sitio ofrece `posiciones_consolidacion × contenedores_por_posicion_dia` contenedores por día; los pendientes compiten por ese cupo en orden de fecha límite del pedido y el que no entra espera al día siguiente acumulando `ContenedorExportacion.diasEsperaPosicion`.
- Parámetro `Main.estrategiaConsolidacion` y opción `CONSOLIDACION_TERMINAL` en `EstrategiaLogistica`: la corrida elige dónde se estiba el contenedor. En pantalla aparecen los contenedores esperando posición, la espera acumulada en contenedor-día y el uso de las posiciones ofrecidas.
- Columnas `posiciones_consolidacion` y `contenedores_por_posicion_dia` en la tabla `Ubicacion`, con `DatosEntrada.capacidadConsolidacionDia(idUbicacion)`. Un sitio de consolidación con capacidad cero no retrasa el despacho: lo detiene para siempre, así que `DatosEntrada.validar()` lo trata como error de datos.
- `Inventario.validar()` y `Main.validarInventario()`: los invariantes de las capas (toneladas no negativas, reservado no mayor que el saldo físico, ubicación válida) se verifican todos los días y abortan la corrida con el detalle si se rompen.
- `Main.abrirPosicionesCrossDockDelDia()`, `Main.capacidadCrossDockLibre(idUbicacion)`, `Main.tomarPosicionesCrossDock(idUbicacion, cantidad)`, `Main.seleccionarSitioCrossDock(pedido)`, `Main.intentarCrossDockPedido(pedido)` y `Main.programarCrossDockDelDia()` (fase 8, ADR-010, ADR-011, ADR-041): el cross docking es una operación con cupo diario por sitio. Los pedidos pendientes compiten en orden de fecha límite por producto que todavía está en planta; el que consigue cupo, espacio y camión libre ese mismo día cruza el depósito y se estiba ahí, sin entrar al almacenamiento.
- Parámetro `Main.habilitaCrossDock` y columna `posiciones_cross_dock` en `Ubicacion`, con `DatosEntrada.capacidadCrossDockDia(idUbicacion)`: la corrida decide si el cross dock existe, y cada sitio publica cuántas operaciones por día puede hacer. En pantalla aparecen las operaciones, las toneladas cruzadas, el costo, los pedidos que no pudieron cruzarse y los cruces degradados.
- Columna `tipo_servicio` (`CONSOLIDACION` o `CROSS_DOCK`) en `TarifaServicioCarga`, con `DatosEntrada.servicioCargaUsdTn(idUbicacion, producto, tipoServicio)`, `Deposito.getCostoCrossDock(producto)` y `Main.costoServicioEstibaUsdTn(pedido, deposito)`: quién cobra la estiba y a qué tarifa se resuelve en un solo lugar, usado tanto por el costo estimado del contenedor como por el costo real del envío.
- El [contrato de datos](docs/09_Definicion/Contrato_de_Datos.md) documenta las hojas y encabezados que lee el importador, y las tablas del esquema objetivo que todavía no consume el modelo (`TiemposOperativos`, tarifas por contenedor, `LoteInicial` y los campos completos de `Ubicacion`, `Distancia` y `PedidoPlan`).
- Experimento `Escenarios` (Parameter Variation freeform, fase 13, ADR-042): 12 escenarios × 30 réplicas, con `idEscenario` y `replica` como únicas dimensiones y la semilla calculada dentro del modelo como `semillaBase + replica`. Escribe `resultados/kpis_por_corrida.csv` con una fila por corrida —`version_modelo`, `id_escenario`, `replica`, `semilla` y los diez KPIs— e imprime al terminar la media, el desvío, el mínimo, el máximo y el P95 de cada KPI por escenario, con el delta absoluto y porcentual contra E-00. Verificado en PLE: 360 corridas en `Finished`, alrededor de un minuto.
- KPIs de corrida como funciones de `Main`: `costoTotalCampania()`, `toneladasExportadas()`, `costoPorToneladaExportada()`, `nivelServicio()`, `atrasoPromedioDias()`, `utilizacionFlota()`, `excedenteFinalTn()` y `usoPosicionesConsolidacion()`.
- Parámetro `Main.replica` y función `Main.aplicarEscenario()`: la corrida sabe qué réplica es y traduce la fila del escenario a duración de campaña, cross dock y estrategia de consolidación.
- Palancas de escenario en la tabla `Escenario` (y en la hoja homónima del Excel): `camiones_producto`, `camiones_portacontenedor`, `factor_produccion`, `factor_capacidad_planta`, `factor_capacidad_deposito`, `factor_storage`, `ventana_demanda`, `habilita_cross_dock`, `deterministico` y `estrategia_consolidacion`. Con ellas, `GeneradorSintetico.ESCENARIOS` define E-00 a E-11 como filas: agregar un escenario no toca el experimento.
- Flota de producto como capacidad diaria (ADR-044): `Main.abrirFlotaDelDia()`, `camionDiaViaje(origen, destino)`, `viajesNecesariosCamion(toneladas)`, `flotaProductoLibreHoy()`, `tomarFlotaProducto(idDestino, viajes)` y `flotaProductoAlcanza(idDestino, toneladas)`. Cada día se ofrecen `camiones_producto` camión-día; un viaje redondo consume `2 × distancia / velocidad / horas_operativas_dia` de jornada y mueve a lo sumo `capacidad_camion_tn`. `tomarFlotaProducto()` aborta la corrida si la capacidad del día se sobregira (V-026).
- Columnas `camiones_portacontenedor`, `capacidad_camion_tn`, `velocidad_camion_kmh` y `horas_operativas_dia` en la tabla `Escenario`, validadas al arrancar. La velocidad del camión dejó de estar cableada en el cálculo del tiempo de viaje.
- KPIs `utilizacion_portacontenedor` (`Main.utilizacionPortacontenedor()`) y `viajes_planta_deposito` (`Main.viajesPlantaDeposito`) en `resultados/kpis_por_corrida.csv`.
- Tablero de indicadores en la vista de `Main` (fase 15, ADR-045): ocho paneles —campaña, producción y stock, transporte y flota, pedidos y servicio, inventario y reservas, contenedores y envíos, consolidación y cross dock, costos— y cinco gráficos de evolución: stock en planta, stock por depósito, utilización de las dos flotas, costos acumulados y pedidos por estado. Reemplaza los 54 textos sueltos que se superponían con el diagrama de flujo. Ningún indicador recalcula nada: cada línea llama a la misma función de `Main` que alimenta el CSV del barrido, así que tablero y barrido no pueden discrepar. Las variables y los parámetros de `Main` dejan de dibujarse en la corrida.
- [Manual de uso](docs/10_Manual/Manual_de_Uso.md): paso a paso para abrir el modelo, correr una campaña, cargar datos propios desde Excel, definir escenarios, correr el barrido, leer `resultados/kpis_por_corrida.csv` y resolver los errores frecuentes, con recetas para dimensionar flota y depósitos.
- [Tablero e indicadores](docs/10_Manual/Tablero_e_Indicadores.md): qué muestra cada panel y cada gráfico, la definición formal de los doce KPIs del barrido y cómo leerlos sin equivocarse.
- Tablero del barrido en la pantalla del experimento `Escenarios`, que estaba vacía (fase 16, ADR-046): avance de las corridas, lectura del barrido (mejor servicio, menor costo por tonelada y el más barato con servicio ≥ 95%), medias por escenario con la configuración que le corresponde y el desvío al lado de la media, y frente de decisión con barras relativas al rango del barrido y marca de escenario dominado. Todo se calcula sobre `corridas`, la misma colección que se escribe en el CSV.
- `Corrida.config` en el experimento: cada corrida guarda camiones de producto y portacontenedor, factores de capacidad de depósito y de producción, cross dock y sitio de consolidación leídos de `root.datos.escenario`, así que la tabla del tablero describe lo que el modelo corrió.
- Funciones del experimento `corridasDe`, `configDe`, `mediaKpi`, `desvioKpi`, `escenariosConDatos`, `escalar`, `barra`, `fraccionCompletada`, `escenarioEnCurso`, `tablaEscenarios`, `frenteDecision` y `recomendaciones`.
- Lote comercial acumulativo (fase 2, ADR-047): `LoteProducto` suma `cliente`, `calidad`, `toneladasObjetivo` y `estadoComercial`, con la lista de opciones nueva `EstadoComercialLote` (`ABIERTO`/`CERRADO`). `Main.buscarLoteComercialAbierto(producto, cliente, calidad)` devuelve el lote abierto compatible —a lo sumo uno por combinación— y `crearLoteEnPlanta()` acumula sobre él: la producción diaria entra como una **capa nueva del mismo `idLote`** en lugar de crear una identidad comercial por día. El lote se cierra cuando la producción acumulada alcanza `toneladasObjetivo`; el despacho y la transferencia parcial no lo cierran, y sólo cuando está cerrado la producción siguiente abre una identidad nueva. `toneladasIniciales` pasa a significar producción acumulada del lote y nunca se reduce; la existencia física sigue viviendo únicamente en las capas del `Inventario`. Casos V-003 y V-004 cumplen.
- Columna `toneladas_objetivo_lote_tn` en la tabla `Producto` y columnas `cliente_default` y `calidad_default` en `Escenario`: los datos comerciales del lote se cargan por el mismo camino que el resto del contrato, sin nada cableado en código. Sintético: objetivo 2.000 tn para jugo, 1.200 para cáscara y 200 para aceite, cliente `GENERICO` y calidad `ESTANDAR`. Un objetivo de 0 significa que el lote no cierra por tamaño.
- Frío propio sin pérdida de producto (fase 19, ADR-048): `Planta.producir()` ingresa la producción completa del plan y la capacidad nominal pasa a ser un umbral de lectura. Desaparecen `excedente*`, `nivelActivacion*` y `stockObjetivo*`; los umbrales viven en el escenario como porcentajes (`umbral_alerta_pct` 85, `umbral_objetivo_pct` 100, `umbral_sobrecarga_pct` 105). `Main` mide la ocupación una vez por día en `registrarOcupacionPlanta()` y acumula `tonDiaSobreNominalPlanta`, `tonDiaSobreCriticoPlanta`, `diasSobrecargaPlanta` y `picoOcupacionPlantaPct`.
- Política de transferencia desde planta gobernada por el escenario (ADR-048): `revisarTransferenciasPlanta()` delega en `toneladasASacarDePlanta()` (`FLEXIBLE`: retener en frío propio y sacar sólo lo que exijan el forecast del horizonte y las obligaciones pendientes) o en `toneladasASacarReactiva()` (comportamiento anterior con umbrales en porcentaje). El forecast es perfecto sobre el plan de producción, con horizonte `dias_forecast` (default 7): `forecastProduccion()` y `demandaProyectada()`.
- Costo económico separado del costo de caja (ADR-049): `devengarOportunidadFrioPropio()`, `costoTotalEconomico()` y `costoEconomicoPorTonelada()`. El costo de caja no cambia de definición; el económico le suma el costo de oportunidad del frío propio y la penalidad por tonelada-día sobre el nivel nominal. Con las dos tarifas en 0 los dos números coinciden.
- Columnas nuevas del contrato (fase 18): `umbral_alerta_pct`, `umbral_objetivo_pct`, `umbral_sobrecarga_pct`, `dias_forecast` y `politica_frio_propio` en `Escenario`; `contenedores_por_dia` en `Ubicacion`, que reemplaza a `posiciones_consolidacion × contenedores_por_posicion_dia`; `oportunidad_usd_tn_dia` y `penalidad_sobrecarga_usd_tn_dia` en `TarifaAlmacenamiento`. Generador sintético, validaciones, importador de Excel, `tools/VolcarDatos.java` y plantilla acompañan el cambio.
- Escenario E-12: `politica_frio_propio = REACTIVA` contra el default `FLEXIBLE`. El barrido pasa a 13 escenarios × 30 réplicas = 390 corridas.
- Seis KPIs nuevos en `resultados/kpis_por_corrida.csv`: `costo_oportunidad_frio_usd`, `costo_total_economico_usd`, `costo_economico_usd_tn`, `ton_dia_sobre_nominal`, `dias_sobrecarga` y `pico_ocupacion_planta_pct`.
- ADR-048 y ADR-049; caso de validación V-002 reescrito y V-002b nuevo.
- Campo `deterministico` del escenario (ADR-043): E-09 deja de sortear el día de llegada, el plazo, el producto y la terminal de cada pedido, no sólo la magnitud. Verificado en PLE: desvío exactamente 0 del costo total en 30 réplicas, contra 4,2% antes del cambio.

### Cambiado

- **La planta ya no descarta producto y por eso el barrido se mueve otra vez (ADR-048, ADR-049).** Medido sobre 30 réplicas de los 13 escenarios: el costo de caja baja entre 6,9 % y 14,3 % y el nivel de servicio sube (E-00 de 0,941 a 0,987, E-07 de 0,606 a 0,972), porque con el frío propio flexible el producto sale más tarde de la planta y paga menos almacenaje de terceros mientras queda disponible cerca del origen. La sobrecarga aparece donde falta capacidad: E-01 acumula 452 tn-día sobre el nivel nominal con un pico de 104,3 % de ocupación; el resto queda en 100 %. **Las series anteriores a `fase-19` no son comparables con las nuevas.**
- **`excedente_final_tn` cambia de significado**: es el stock que queda en la red al cierre de la campaña, no producto perdido. El faltante de frío se lee en `ton_dia_sobre_nominal`, `dias_sobrecarga` y `pico_ocupacion_planta_pct`.
- `datos/entrada_ejemplo.xlsx` regenerado con el contrato de la fase 18: un libro anterior deja de importar. Los valores del generador se redondean a seis decimales de tonelada (~1 g) para que la corrida desde Excel y desde el generador sean numéricamente idénticas; comparadas celda por celda, las 743 filas dan 0 diferencias.
- `VERSION_MODELO` del experimento pasa de `fase-13` a `fase-16`, `fase-17` y ahora `fase-19`: el CSV del barrido queda etiquetado con la versión que lo produjo.
- El manual documenta que `Escenarios` se corre con botón derecho → `Run` (con `F5` AnyLogic repite el último experimento usado) y agrega la lectura del tablero del barrido.
- **Los KPIs del barrido se mueven con el lote acumulativo (ADR-047), y no por un cambio de negocio.** El flete y la flota planta–depósito se cuentan por viajes, y los viajes son `ceil(toneladas / capacidad_camion_tn)` por llamada a `transferirToneladasLote()` (ADR-044). Con un lote por día, cada llamada movía como máximo la producción de un día y el redondeo del último camión parcial se pagaba una vez por día y por producto: con camión de 25 tn, la cáscara (60 tn/día) gastaba 3 viajes donde necesitaba 2,4 y el aceite (8 tn/día) gastaba 1 viaje entero para 0,32. Al acumular, el redondeo ocurre una vez por tanda. Medido sobre 30 réplicas en los doce escenarios: **12% a 16% menos viajes planta–depósito**, 0,3% a 2,6% menos costo total, toneladas y contenedores exportados prácticamente iguales (+0,05%), y el mayor efecto en los escenarios con flota escasa (E-01 baja el atraso medio de 3,17 a 2,20 días). La respuesta de dimensionamiento no cambia, pero **las series anteriores a `fase-17` no son comparables con las nuevas**.
- `datos/entrada_ejemplo.xlsx` regenerado: la hoja `Producto` suma `toneladas_objetivo_lote_tn` y la hoja `Escenario` suma `cliente_default` y `calidad_default`. Un libro anterior deja de importar; hay que regenerarlo con `python3 tools/generar_excel_ejemplo.py` y volver a cargar los valores propios.

- Modelo, datos de entrada como tablas (paso 2, H-08 parcial, H-09, H-10, H-11):
  - la demanda deja de ser tres pedidos cableados y se lee de la tabla `PedidoPlan`;
  - la producción diaria de la planta se lee de `ProduccionPlan` en lugar de tres parámetros fijos;
  - distancias, capacidades, tarifas de almacenaje, fletes a terminal, consolidación, velocidades y el tipo y la capacidad de contenedor por producto se leen de las tablas; los depósitos y terminales pasan a identificarse por `idUbicacion`;
  - una tarifa o una distancia faltante aborta el arranque con la clave que falta, en lugar de valer cero.
  - El [contrato de datos](docs/09_Definicion/Contrato_de_Datos.md) documenta qué tablas están implementadas y en qué se apartan del esquema objetivo. Falta el importador de Excel, que llenará las mismas listas.
- Modelo, higiene previa a la migración de datos e inventario:
  - 39 parámetros que en realidad eran estado de corrida pasaron a variables en `Main`, `Planta`, `Deposito` y `Terminal` (H-02, ADR-033). Los parámetros que quedan son las entradas del modelo.
  - Los seis eventos diarios de `Main` y `Planta.produccionDiaria` se reemplazan por un único evento `pasoDiario` que invoca las siete fases en orden fijo (H-06, H-07, ADR-034). Desaparecen las cadencias de 1,2 y 1,9 días.
  - El almacenaje se devenga una sola vez, en `devengarAlmacenamientoDiario()`: se imputa por lote y se agrega al depósito en el mismo recorrido, e incluye los lotes reservados (H-04).
- Modelo, inventario por capas (paso 3, H-03, H-05, ADR-021 a ADR-025). Verificado en PLE: el escenario E-00 da exactamente los mismos 494 envíos, 40 pedidos entregados, USD 1.270.080 de almacenaje, USD 212.700 de flete depósito–puerto y USD 143.642 de consolidación que antes de la migración, de modo que el cambio es de representación y no de resultados.
  - `Planta.stock*` y `Deposito.stock*`/`reservado*` dejan de existir: `getStock()` y `getReservado()` derivan de las capas (ADR-023). Con ellos se van `agregarStock`, `retirarStock`, `recibirProducto`, `retirarProducto`, `reservarProducto`, `liberarReserva` y `despacharReservado`, porque ya no hay dos saldos que sincronizar.
  - Reservar deja de partir el lote en dos agentes: `crearLoteReservadoDesdeDivision` se eliminó (H-05). La reserva es un registro `(codigoPedido, toneladas, díaReserva)` sobre las capas más antiguas del depósito, así que `toneladasIniciales` vuelve a significar lo producido y el `idLote` no se fragmenta.
  - El lote pierde `toneladasDisponibles`, `toneladasReservadas` y `loteOrigen`; en su lugar `getToneladasDisponibles()`, `getToneladasReservadas()` y `getToneladasLibres()` consultan sus capas.
  - El almacenaje se devenga por capa —que es lo que tiene ubicación y día de ingreso propios— y se imputa al lote y al depósito en el mismo recorrido, sin reintroducir el doble conteo de H-04.
- Modelo, transferencia parcial planta → depósito (fase 4). `transferirLoteCompleto` se eliminó: ahora se manda `min(saldo libre del lote, pendiente)` y, si en el depósito elegido no entra todo, entra lo que quepa y el resto sale en la vuelta siguiente, posiblemente a otro depósito.
  - `transferirProductoADepositos` ya no corta el ciclo cuando el lote es más grande que el pendiente, y `seleccionarDeposito` compara costo estimado **por tonelada** de lo que efectivamente entra en lugar de descartar todo depósito donde no entre la carga completa. Las dos reglas anteriores dejaban producto en planta con capacidad libre repartida entre depósitos.
  - `buscarLoteMasAntiguoEnPlanta` filtra por saldo real en planta y no por `estado`/`ubicacionActual`, para que un lote transferido a medias pueda terminar de salir.
  - `ubicacionActual` pasa a ser la ubicación de mayor saldo del lote, no "donde está todo".
  - Efecto en E-00, verificado en PLE: se transfieren 25.558 tn en 405 viajes contra 25.164 tn en 374, y el almacenaje sube de USD 1.270.080 a USD 1.310.327 porque el producto sale antes de la planta y pasa más días en depósito. Lo entregado no cambia (494 envíos, 40 pedidos, 6.801 tn de jugo, 5.056 de cáscara, 64 de aceite).
- Modelo, contenedores individuales (fase 6). `generarEnviosParaPedido` deja de partir la carga en camiones genéricos de 25 tn cableadas: crea los contenedores del pedido y genera un envío por contenedor, con `Envio.contenedor` como vínculo. La capacidad sale de la tabla `Producto`, así que cambiarla es un dato y no código.
  - El contenedor recorre sus estados con el envío: `ESPERANDO_PROGRAMACION` al crearse, `ESPERANDO_CARGA` al generarse el envío, `CONSOLIDANDO` al cargar, `EN_TRANSITO_CARGADO` al salir, `INGRESADO_TERMINAL` al llegar y `EXPORTADO` al cerrar, con sus tiempos y su costo real.
  - Se eliminan las pruebas técnicas que reemplaza esta lógica: `Main.pruebaCrearContenedor()`, `Pedido.probarCalculoContenedores()` y el botón que la invocaba.
  - Efecto en E-00, verificado en PLE: 494 contenedores, los 494 exportados, y el resto de los indicadores sin cambios respecto de la fase 4. La capacidad sólo difiere de las 25 tn anteriores en el aceite (20 tn), y los dos pedidos de aceite del escenario (30,2 y 34,0 tn) necesitan dos contenedores con cualquiera de las dos capacidades; cambia el reparto de toneladas entre contenedores, no la cantidad. El costo tampoco se mueve porque el flete se cobra por viaje y la consolidación por tonelada.
- Modelo, consolidación directa (fase 7). Con `CONSOLIDACION_DEPOSITO`, que es el valor por defecto, la carga en el depósito **es** la consolidación: el tiempo sale de `velocidad_consolidacion_tn_hora` del depósito, el costo de la tarifa de estiba del depósito, el acumulado se imputa al depósito y el bloque `consolidarCarga` queda de paso. Con `CONSOLIDACION_TERMINAL` rige el comportamiento anterior. La elección de depósito también cambia, porque el costo estimado del pedido incluye la tarifa de estiba del depósito candidato.
  - Los envíos ya no se generan todos juntos al reservar el pedido: `despacharContenedoresPendientes()` crea uno por contenedor a medida que consigue posición, de modo que un pedido puede despachar a lo largo de varios días. `generarEnviosParaPedido()` se elimina.
  - Efecto en E-00, verificado en PLE (`Finished`, día 365): 494 contenedores, los 494 exportados, 40 pedidos entregados y las mismas 25.558 tn transferidas en 405 viajes. La consolidación baja de USD 143.642 a USD 94.059 porque la tarifa de estiba del depósito es menor que la de la terminal; el almacenaje sube de USD 1.310.327 a USD 1.325.420 y el flete depósito–puerto de USD 211.932 a USD 215.916, porque las posiciones limitan la salida (1.491 contenedor-día de espera, 7% de uso de las posiciones ofrecidas) y cambia el reparto entre depósitos.
  - La misma corrida con `CONSOLIDACION_TERMINAL` reproduce los valores de la fase 6 (USD 143.642 de consolidación, USD 211.932 de flete) salvo USD 398 más de almacenaje, que son los 46 contenedor-día que ahora esperan posición en la terminal.
- Modelo, cross docking (fase 8). La secuencia diaria resuelve el cross dock antes de las transferencias normales: lo que cruza sale de planta contra un pedido concreto y sólo el excedente se guarda. El devengo diario de almacenaje excluye las toneladas reservadas por pedidos cruzados, de modo que cruzar no genera storage (ADR-010), y el contenedor de un pedido cruzado tiene prioridad de despacho sobre el resto.
  - Los depósitos sintéticos ofrecen entre 0 y 16 operaciones de cross dock por día, más que sus posiciones de consolidación, porque cruzar es más rápido que estibar desde stock; uno de los cinco no cruza.
  - Efecto en E-00, verificado en PLE (`Finished`, día 365). Con `habilitaCrossDock = false` los indicadores son exactamente los de la fase 7 (USD 94.059 de consolidación, USD 1.325.420 de almacenaje, USD 215.916 de flete depósito–puerto, 1.491 contenedor-día de espera, 7% de uso de posiciones, costo total USD 1.471.989). Con `habilitaCrossDock = true` se cruzan 7.581 tn en 316 operaciones por USD 53.895: la consolidación baja a USD 34.723, el almacenaje a USD 1.285.612, el flete depósito–puerto a USD 214.044, la espera a 607 contenedor-día y el uso de posiciones al 2%, con un costo total de USD 1.432.360 (2,7% menos). Los dos casos entregan lo mismo: 494 contenedores exportados y 40 pedidos.
  - Limitaciones: la reserva sigue siendo todo-o-nada, así que un pedido se cruza entero o no se cruza (14 quedaron sin cruzar en E-00); el cross dock en terminal (`CROSS_DOCK_TERMINAL`) y las colas propias del sitio siguen pendientes.
- Modelo, escenarios y réplicas (fase 13). `GeneradorSintetico.generar(idEscenario, semilla)` reemplaza a la firma con ocho argumentos: el escenario es ahora una fila que se aplica a los datos, y `Main` ya no puede pedir una combinación de parámetros que no corresponda a ningún escenario. `tools/VolcarDatos.java` y `tools/generar_excel_ejemplo.py` acompañan el cambio y la plantilla `datos/entrada_ejemplo.xlsx` trae las doce filas de escenario.
  - **Contrato de Excel roto:** la hoja `Escenario` tiene nueve columnas nuevas y el importador las exige. Un libro anterior falla la importación con la lista de columnas faltantes; hay que regenerarlo con `python3 tools/generar_excel_ejemplo.py` y volver a cargar los valores propios.
  - No cambia el comportamiento de la fase 8: E-00 réplica 0 (semilla 1) da los mismos 494 contenedores exportados que la fase 7/8 sin cross dock, y E-05 réplica 0 cruza las mismas 7.580,5 tn que la corrida de cross dock de la fase 8.
  - La limitación del barrido de flota que dejaba E-01 y E-02 idénticos a E-00 se resolvió con ADR-044 (ver más abajo).
- Modelo, consumo real de flota (ADR-044). La transferencia planta→depósito toma camiones y el pool del flujo pasa a ser la flota de portacontenedores (`flotaCamiones` → `flotaPortacontenedores`, con capacidad `camiones_portacontenedor`). `camionDisponibleHoy()` desaparece: el cross dock exige que la flota del día alcance para el pedido entero, y `transferirToneladasLote()` mueve sólo lo que entra en los viajes que quedan, dejando el resto en planta.
  - El flete planta→depósito se cobra **por viaje**: `calcularCostoPlantaDeposito()` multiplica el costo fijo y el kilometraje por la cantidad de viajes, en lugar de cobrar un viaje por transferencia sin importar el tonelaje.
  - `utilizacionPortacontenedor()` usa la estadística del pool en vez del muestreo diario, que medía 0,002 con la flota trabajando porque no veía los viajes que empiezan y terminan el mismo día. Se eliminan `registrarUsoFlota()`, `camionDiaPortacontenedorOcupado` y `camionDiaPortacontenedorOfrecido`.
  - Corregido: el `Delay` de retorno del camión usaba segundos en lugar de horas, así que el portacontenedor quedaba libre en el mismo instante en que salía.
  - E-01 y E-02 pasan a ser 1 y 6 camiones de producto (1 y 8 portacontenedores) sobre un caso base de 3 y 4. Verificado en PLE con el barrido completo (360 corridas `Finished`, E-09 con desvío 0): E-01 da nivel de servicio 0,908 y atraso 3,17 días contra 0,940 y 1,93 de E-00, mientras E-02 no mejora a E-00 (0,939 y 1,88). La utilización de la flota de producto va de 0,399 (E-01) a 0,070 (E-02).
  - Efecto a tener en cuenta al leer costos: el costo total **sube** con el tamaño de la flota (1,76 M → 1,83 M → 1,88 M USD), porque el producto llega antes al depósito y paga más almacenaje mientras el excedente que espera camión en planta no paga nada. El punto de quiebre de la flota se lee en servicio y atraso, no en costo.
- ADR-020: la licencia queda resuelta. El proyecto se desarrolla dentro de PLE. Se corrige el supuesto anterior de que PLE impedía los barridos con réplicas: Parameter Variation y Monte Carlo están disponibles, y el límite de 5 horas de tiempo de modelo no aplica a la Process Modeling Library.
- ADR-021: se precisa que el devengo diario de almacenaje no requiere capas; lo que sí las requiere es la imputación de IN/OUT parciales, el consumo por antigüedad y evitar partir el lote en agentes.
- Roadmap: fase 3 corregida de 30% a 0% tras verificar que las listas por ubicación no existen en el modelo (H-03).

### Definiciones previas

- Definición del proyecto como modelo de dimensionamiento de campaña, contrato de datos de entrada, escenarios E-00 a E-10, glosario y ADR-018 a ADR-029 ([PR #1](https://github.com/mazzuccoda/Anylogic_log_arg_2026/pull/1)).
